ARG BASE_IMAGE=coding-agent-base
FROM ${BASE_IMAGE}

USER root

RUN npm install -g @google/gemini-cli

RUN mkdir -p /home/coding-agent/.gemini \
    && chown -R coding-agent:coding-agent /home/coding-agent/.gemini

USER coding-agent

ENV AGENT=gemini-cli
